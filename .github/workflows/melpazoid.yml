# melpazoid <https://github.com/riscy/melpazoid> build checks.

# If your package is on GitHub, enable melpazoid's checks by copying this file
# to .github/workflows/melpazoid.yml and modifying RECIPE and EXIST_OK below.

name: melpazoid
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - package: project-hercules
    steps:
    - uses: actions/checkout@v2
    - uses: cachix/install-nix-action@v17
    - name: Set up Python 3.6
      uses: actions/setup-python@v1
      with: { python-version: 3.6 }
    - name: Install
      run: |
        python -m pip install --upgrade pip
        sudo apt-get install emacs && emacs --version
        git clone https://github.com/riscy/melpazoid.git ~/melpazoid
        pip install ~/melpazoid

    - name: Set the recipe file
      run: |
        echo "RECIPE_FILE=$(
          nix flake prefetch melpa --inputs-from . --json | jq -r .storePath
        )/recipes/${{ matrix.package }}" >> $GITHUB_ENV

    - name: Run
      env:
        LOCAL_REPO: ${{ github.workspace }}
        # set this to false (or remove it) if the package isn't on MELPA:
        EXIST_OK: true
      run: echo $GITHUB_REF && make -C ~/melpazoid
